name: "mera pehla workflow"
on:
  push: 
    branches:
      - main
jobs:
  terraformInit:
    name: dhondu
    runs-on: actionLabel
    steps:
     - name: Run echo in cmd
       run: echo dhondu
       shell: cmd

      
      
